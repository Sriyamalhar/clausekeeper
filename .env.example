# Postgres connection string (Supabase, Neon, or local)
DATABASE_URL="postgresql://user:password@localhost:5432/clausekeeper"

# Auth.js
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# OAuth (optional, for Google login)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Anthropic API for AI clause extraction (server-side only, never expose)
ANTHROPIC_API_KEY=""

# File storage for uploaded contract PDFs (Vercel Blob)
BLOB_READ_WRITE_TOKEN=""
